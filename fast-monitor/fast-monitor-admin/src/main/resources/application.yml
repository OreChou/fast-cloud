server:
  port: 8401

spring:
  application:
    name: Fast-Monitor-Admin
  boot:
    admin:
      ui:
        title: ${spring.application.name}
  security:
    user:
      name: fast
      password: 123456